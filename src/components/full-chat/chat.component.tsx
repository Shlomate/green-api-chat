import { FC } from 'react'; import { Flex } from 'antd'; import ContentSide from './content-side/content-side.component'; import { useAppSelector } from 'hooks'; import { selectType } from 'store/slices/chat.slice'; const Chat: FC = () => { const type = useAppSelector(selectType); return ( <Flex className={`full-chat full-chat--single ${type === 'console-page' ? 'console-page' : ''}`} style={{ overflowY: 'hidden' }} > <ContentSide /> </Flex> ); }; export default Chat;
